export {
  circle,
  rect,
  ellipse,
  line,
  polygon,
  path,
  text,
  image,
  animate,
  withStroke,
  transform,
} from './elements';

export { render } from './renderer';

export type { SvgDef, EventHandler } from './types';
